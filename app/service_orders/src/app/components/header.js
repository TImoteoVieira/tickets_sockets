export default function Header() {
    return (
        <div>
          <h1>
            Welcome
          </h1>
          <p>Websockets + Nextjs + Django + Autentication + Tickets</p>
        </div>
    );
}